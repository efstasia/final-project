import React from 'react';
import styled from 'styled-components';

const HeaderContainer = styled.div`
  height: 150px;
  color: white;
  background: #161616;
`;

export const Header = () => {
  return <HeaderContainer>HEADER FOR page</HeaderContainer>;
};
